interface:
  display_name: "Harness Workflow"
  short_description: "Use for repeatable Harness run sheets with explicit owners, repo-backed artifacts, role-scoped context packs, structured risk review, and gate review"
  default_prompt: "Use $harness-engineering-workflow for repo-backed workflow runs that need explicit owners, validation gates, continuation checkpoints, or independent-context delegation. Start with Fast Tier Check in SKILL.md, then use the tier template and references. Prefer local independent subagents when they provide a real separate context boundary and artifact output."

policy:
  allow_implicit_invocation: false
