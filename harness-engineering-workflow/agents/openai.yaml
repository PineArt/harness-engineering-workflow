interface:
  display_name: "Harness Workflow"
  short_description: "Use for Harness-style execution workflows with explicit owners, repo-backed artifacts, context packs, and gate review"
  default_prompt: "Use harness-engineering-workflow when the task needs a Harness-style execution workflow with explicit owners, repo-backed artifacts, role-scoped context packs, structured risk review, gate decisions, or environment-first coordination. Start with Fast Tier Check in SKILL.md to choose Ultra Lite, Lite, or Full. Skip plain one-off coding that does not need workflow artifacts, validation surfaces, or repeatable operating rules."

policy:
  allow_implicit_invocation: false
