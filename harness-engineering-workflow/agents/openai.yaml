interface:
  display_name: "Structured Execution Workflow"
  short_description: "Use for multi-agent or gated execution work with explicit owners, context packs, workflow artifacts, and repo-backed validation"
  default_prompt: "Use harness-engineering-workflow when the task needs a reusable execution workflow with explicit owners, context packs, workflow artifacts, structured risk review, gate decisions, or environment-first coordination. Start with Fast Tier Check in SKILL.md to choose Ultra Lite, Lite, or Full. Skip it for plain one-off coding with no reusable workflow surface."

policy:
  allow_implicit_invocation: false
